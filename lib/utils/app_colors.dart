import 'package:flutter/material.dart';

class AppColors {
  // Background principal - preto bem escuro como no Figma
  static const Color background = Color(0xFF0D0D0F);
  static const Color surface = Color(0xFF13131A);       // cards / containers
  static const Color surfaceLight = Color(0xFF1A1A24);  // header border, separadores

  // Acentos
  static const Color primary = Color(0xFF6366F1);       // Indigo principal
  static const Color primaryDark = Color(0xFF4F46E5);
  static const Color accent = Color(0xFF8B5CF6);        // Roxo
  static const Color accentHover = Color(0xFFA78BFA);

  // Texto
  static const Color textPrimary = Color(0xFFF1F5F9);
  static const Color textSecondary = Color(0xFFCBD5E1);
  static const Color textMuted = Color(0xFF64748B);

  // Bordas sutis
  static const Color border = Color(0xFF1E1E2E);
  static const Color borderLight = Color(0xFF2A2A3A);

  // Estados
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
}
